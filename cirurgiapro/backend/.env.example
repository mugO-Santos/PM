# Neon PostgreSQL — copie a connection string do painel do Neon
DATABASE_URL=postgresql://user:password@ep-xxxxx.us-east-2.aws.neon.tech/cirurgiapro?sslmode=require

# Porta do servidor (Railway define automaticamente via PORT)
PORT=3001

# URL do frontend (deixe * para desenvolvimento)
FRONTEND_URL=*
